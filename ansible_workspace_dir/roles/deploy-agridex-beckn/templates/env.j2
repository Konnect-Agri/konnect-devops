# This file is generally used to refer in docker-compose file

# GRAPHQL API VARS
HASURA_URI={{ credentials.HASURA_URI }}
SECRET={{ credentials.SECRET }}

#REDIS Vars
REDIS_URI={{ credentials.REDIS_URI }}

# BASE URIs for APIs
BAP_URI={{ credentials.BAP_URI }}
BG_URI={{ credentials.BG_URI }}
BPP_URI={{ credentials.BPP_URI }}
WEBSOCKET_ADAPTER_URL={{ credentials.WEBSOCKET_ADAPTER_URL }}
PROXY_URL={{ credentials.PROXY_URL }}
TEST_API_URI={{ credentials.TEST_API_URI }}

# MISC env vars (they'll come from beckn registry)
BAP_ID={{ credentials.BAP_ID }}
BPP_ID={{ credentials.BPP_ID }}

# API PORTS
BAP_PORT={{ credentials.BAP_PORT }}
BG_PORT={{ credentials.BG_PORT }}
BPP_PORT={{ credentials.BPP_PORT }}
AGRI_DEX_BECKN_PORT={{ credentials.AGRI_DEX_BECKN_PORT }}
TEST_API={{ credentials.TEST_API }}

AUTHENTICATION_URI={{ credentials.AUTHENTICATION_URI }}
LOGIN_ID={{ credentials.LOGIN_ID }}
PASSWORD={{ credentials.PASSWORD }}
APPLICATION_ID={{ credentials.APPLICATION_ID }}
AUTH_TOKEN_FOR_DC_LOGIN={{ credentials.AUTH_TOKEN_FOR_DC_LOGIN }}